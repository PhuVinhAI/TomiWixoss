import { CardEffect, EffectContext } from "../CardEffect";

export class WaterBuffalo extends CardEffect {
  readonly cardId = "WXDi-D01-018";

  // Const: Multi-color ener payment (Blue/Green) - handled by payment system
}
